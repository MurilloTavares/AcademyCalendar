<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifpb.academycalendar"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="android.support.v4.app.CoreComponentFactory"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/AppTheme" >

        <!-- Main -->
        <activity android:name="br.edu.ifpb.academycalendar.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Cadastrar -->
        <activity android:name="br.edu.ifpb.academycalendar.CadastrarActivity" >
            <intent-filter>
                <action android:name=".CadastrarActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Calendario -->
        <activity android:name="br.edu.ifpb.academycalendar.CalendarioActivity" >
            <intent-filter>
                <action android:name=".CalendarioActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Add evento -->
        <activity android:name="br.edu.ifpb.academycalendar.NovoEventoActivity" >
            <intent-filter>
                <action android:name=".NovoEventoActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Turmas -->
        <activity android:name="br.edu.ifpb.academycalendar.TurmasActivity" >
            <intent-filter>
                <action android:name=".TurmasActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Nova Turma -->
        <activity android:name="br.edu.ifpb.academycalendar.NovaTurmaActivity" >
            <intent-filter>
                <action android:name=".NovaTurmaActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>